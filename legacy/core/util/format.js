
define('util/format', [], function(  ){
	
    var exports = this.exports;
    var util = this.parent;

    exports.phone = function( value ){
        let digits = value.replace(/[^0-9]/g, '');

    }

    return exports;

}, { persistant: true });

